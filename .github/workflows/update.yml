name: update

on:
  schedule:
    - cron: "17 21 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: Recompute both platform hashes even when already current
        required: false
        type: boolean
        default: false
      mode:
        description: push publishes to main directly; pr opens a reviewable pull request
        required: false
        type: choice
        options:
          - push
          - pr
        default: push

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: opencodex-update
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.prepare.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: DeterminateSystems/nix-installer-action@v16
      - name: Prepare source update
        id: prepare
        env:
          GH_TOKEN: ${{ github.token }}
          FORCE_UPDATE: ${{ inputs.force }}
        run: |
          ./scripts/prepare-update.sh
          if [[ "$FORCE_UPDATE" == "true" ]]; then
            echo "Forcing platform hash recomputation"
            python3 - <<'PY'
          import json
          p = "version.json"
          d = json.load(open(p))
          d["bunDepsHash"]["x86_64-linux"] = None
          d["bunDepsHash"]["aarch64-darwin"] = None
          with open(p, "w") as f:
              json.dump(d, f, indent=2)
              f.write("\n")
          PY
          fi
          MISSING_HASH="$(python3 -c '
          import json
          d = json.load(open("version.json"))
          platforms = ("x86_64-linux", "aarch64-darwin")
          print(str(any(d["bunDepsHash"].get(p) is None for p in platforms)).lower())
          ')"
          if git diff --quiet -- version.json bun.lock.normalized \
            && [[ "$MISSING_HASH" == "false" ]]; then
            echo "Already current; no update required"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "Update prepared"
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/upload-artifact@v4
        if: steps.prepare.outputs.changed == 'true'
        with:
          name: prepared-update
          path: |
            version.json
            bun.lock.normalized
          if-no-files-found: error
          retention-days: 1

  hashes:
    needs: prepare
    if: needs.prepare.outputs.changed == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - system: x86_64-linux
            runner: ubuntu-latest
          - system: aarch64-darwin
            runner: macos-14
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/download-artifact@v4
        with:
          name: prepared-update
          path: .
      - uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: sandbox = false
      - name: Compute and verify ${{ matrix.system }} hash
        run: ./scripts/update-platform.sh "${{ matrix.system }}"
      - uses: actions/upload-artifact@v4
        with:
          name: metadata-${{ matrix.system }}
          path: version.json
          if-no-files-found: error
          retention-days: 1

  finalize:
    needs: [prepare, hashes]
    if: needs.prepare.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: prepared-update
          path: .
      - uses: actions/download-artifact@v4
        with:
          name: metadata-x86_64-linux
          path: ${{ runner.temp }}/metadata-linux
      - uses: actions/download-artifact@v4
        with:
          name: metadata-aarch64-darwin
          path: ${{ runner.temp }}/metadata-darwin
      - name: Merge platform hashes
        run: |
          ./scripts/merge-platform-hashes.py \
            version.json \
            "$RUNNER_TEMP/metadata-linux/version.json" \
            "$RUNNER_TEMP/metadata-darwin/version.json"
      - name: Commit update
        id: commit
        run: |
          if git diff --quiet -- version.json bun.lock.normalized; then
            echo "Finalized metadata already matches the source revision"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch --no-tags origin "+refs/heads/main:refs/remotes/origin/main"
          OLD_VERSION="$(git show origin/main:version.json | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])')"
          NEW_VERSION="$(python3 -c 'import json; print(json.load(open("version.json"))["version"])')"
          if [[ "$OLD_VERSION" != "$NEW_VERSION" ]]; then
            MESSAGE="chore: bump opencodex to ${NEW_VERSION}"
          else
            MESSAGE="chore: refresh opencodex ${NEW_VERSION} build metadata"
          fi
          git add version.json bun.lock.normalized
          git commit -m "$MESSAGE"
          echo "version=${NEW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "changed=true" >> "$GITHUB_OUTPUT"
      - name: Publish to main
        if: steps.commit.outputs.changed == 'true' && (inputs.mode || 'push') == 'push'
        run: |
          for attempt in 1 2 3; do
            git fetch --no-tags origin "+refs/heads/main:refs/remotes/origin/main"
            if git rebase origin/main && git push origin "HEAD:refs/heads/main"; then
              exit 0
            fi
            git rebase --abort 2>/dev/null || true
            echo "push attempt ${attempt} failed; retrying"
            sleep 15
          done
          echo "could not publish the update to main" >&2
          exit 1
      - name: Push update branch
        if: steps.commit.outputs.changed == 'true' && inputs.mode == 'pr'
        run: |
          git push origin "HEAD:refs/heads/bot/opencodex-update" --force
      - name: Create or update pull request
        id: pr
        if: steps.commit.outputs.changed == 'true' && inputs.mode == 'pr'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git fetch --no-tags origin "+refs/heads/bot/opencodex-update:refs/remotes/origin/bot/opencodex-update"
          OLD_VERSION="$(git show origin/main:version.json | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])')"
          NEW_VERSION="$(git show origin/bot/opencodex-update:version.json | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])')"
          if [[ "$OLD_VERSION" != "$NEW_VERSION" ]]; then
            TITLE="chore: bump opencodex to ${NEW_VERSION}"
          else
            TITLE="chore: refresh opencodex build metadata (${NEW_VERSION})"
          fi
          BODY="Automated update of the pinned opencodex version/hashes by the daily update workflow.

          - Version: ${NEW_VERSION}
          - src hash and x86_64-linux / aarch64-darwin bunDeps hashes refreshed as needed
          - normalized bun.lock regenerated on version bumps

          Both platforms were built and verified in the update workflow before this PR was opened. Auto-merge is enabled, so no review is required to publish."
          OPEN_PR_NUMBER="$(gh pr list \
            --base main \
            --head bot/opencodex-update \
            --state open \
            --json number \
            --jq '.[0].number // empty')"
          if [[ -n "$OPEN_PR_NUMBER" ]]; then
            echo "Updating open PR #${OPEN_PR_NUMBER}"
            gh api \
              --method PATCH \
              "repos/${GITHUB_REPOSITORY}/pulls/${OPEN_PR_NUMBER}" \
              -f title="$TITLE" \
              -f body="$BODY" \
              >/dev/null
          else
            echo "Creating PR from bot/opencodex-update"
            OPEN_PR_NUMBER="$(gh api \
              --method POST \
              "repos/${GITHUB_REPOSITORY}/pulls" \
              -f title="$TITLE" \
              -f head="bot/opencodex-update" \
              -f base="main" \
              -f body="$BODY" \
              --jq .number)"
          fi
          echo "number=${OPEN_PR_NUMBER}" >> "$GITHUB_OUTPUT"
      - name: Enable auto-merge
        if: steps.commit.outputs.changed == 'true' && inputs.mode == 'pr'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_NUMBER="${{ steps.pr.outputs.number }}"
          for attempt in 1 2 3; do
            if gh pr merge "$PR_NUMBER" --squash --auto; then
              echo "Auto-merge enabled on PR #${PR_NUMBER}"
              exit 0
            fi
            echo "auto-merge attempt ${attempt} failed; retrying"
            sleep 15
          done
          echo "could not enable auto-merge on PR #${PR_NUMBER}" >&2
          exit 1
